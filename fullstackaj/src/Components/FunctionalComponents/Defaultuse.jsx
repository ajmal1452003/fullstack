var Defaultuse = () =>{
    var styling = {
        color:"red",
        fontSize:"20px",
        textDecoration:"underline"
    }
    return <h1>Default exports are used when you want to export a single primary object, function, or variable from a module. This type of export allows you to import the value using any name, providing flexibility and simplifying the import process for the module’s main content.</h1>
}
export default Defaultuse;